---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: deck
  annotations:
    fluxcd.io/automated: "true"
spec:
  replicas: 3
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  minReadySeconds: 5
  selector:
    matchLabels:
      app: deck
  template:
    metadata:
      labels:
        app: deck
    spec:
      containers:
      - name: deck
        image: 833104194858.dkr.ecr.us-east-1.amazonaws.com/qa-bootcamp_deck:17b7605d5bdff31c19e13e787a39d509b6129931
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: deck
spec:
  selector:
    app: deck
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
