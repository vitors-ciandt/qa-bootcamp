---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: games
  annotations:
    fluxcd.io/automated: "true"
spec:
  replicas: 3
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  minReadySeconds: 5
  selector:
    matchLabels:
      app: games
  template:
    metadata:
      labels:
        app: games
    spec:
      containers:
      - name: games
        image: 833104194858.dkr.ecr.us-east-1.amazonaws.com/qa-bootcamp_games:f5763134dd366388c2b998bdca192c89902e1990
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: games
spec:
  selector:
    app: games
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
